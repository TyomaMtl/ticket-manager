{{ form_start(form_message) }}
    <div class="form-group">
        {{ form_widget(form_message.message, {'attr': {'class': 'form-control', 'placeholder': 'Message'}}) }}
    </div>
    <button class="btn btn-info">{{ button_label|default('Save') }}</button>
{{ form_end(form_message) }}
